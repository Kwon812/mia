{
  "name": "@na/extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "dexie": "^4"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "typescript": "^5",
    "vite": "^7",
    "vitest": "^4.1.10"
  }
}
