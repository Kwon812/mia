/**
 * "최근"이 몇 건인가.
 *
 * 세 화면이 같은 수를 써야 한다.
 *   - 메인(/) 좌하단 '최근 경험' 목록의 줄 수
 *   - 메인 지도에서 잔광을 끄는 경험·기억의 수
 *   - 지도에서 표식을 켜는 경험·갈래의 수
 * 글로 적힌 셋과 빛나는 셋이 어긋나면 둘 다 못 믿게 된다.
 *
 * 컴포넌트가 아니라 여기 두는 이유: 서버 컴포넌트 둘이 이 값으로 쿼리 결과를
 * 자르는데, 클라이언트 컴포넌트에서 가져오면 그 모듈이 통째로 서버 그래프에
 * 딸려 들어온다.
 *
 * ⚠️ orbital-map 의 TRAIL_SPAN 은 순위별 잔광 길이를 배열로 갖고 있다.
 * 이 값을 늘리려면 그 배열도 같이 늘려야 한다 — 안 그러면 넷째부터는
 * 목록에는 뜨는데 지도에서는 아무 표식이 없다.
 */
export const RECENT_LIMIT = 3;
