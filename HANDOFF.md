# HANDOFF

작업이 끊겨도 이어갈 수 있게 남기는 파일. 요약이 아니라 **이게 진실**이다.
컨텍스트가 날아가도 여기만 읽으면 다음 수를 둘 수 있어야 한다.

마지막 갱신: 2026-08-08

---

## ⚑ 진행 중 — 지도를 우주로 (브랜치 `map/cosmos`)

`main` 은 이번 작업 이전 상태다. 되돌리려면 `git checkout main`.

### 이미 한 것 (커밋됨)

```
(HEAD)   feat: 분야를 은하로 — 로그 나선 · halo · 층 배율 · 코스믹 웹  ← 계획 2~5단계
01c17ae  fix: 별이 제 위성보다 작고 어두웠다 — 위계를 불변식으로 못박는다
4d3fced  feat: 갈래 화면을 없애고, 위성을 더 늦게 꺼낸다
306a46d  fix: 남은 것은 '기억'이라고 부른다
08c149f  fix: 크기도 광도도 차이가 안 보였다 — 둘 다 0 을 기준으로 재고 있었다
a824c63  feat: 천체를 하나로 — 갈래가 별이고 기억이 그 광도다   ← 계획 1단계
c3afe23  fix: 시드가 지금 모델과 어긋나 있었다
b429dae  feat: 지도를 별자리로 — 기억이 궤도를 떠나고, 당기면 안이 보인다
852907c  feat: 레일 순서 정리 + 기억 목록을 접는다
```

- 레일: 나 · 기억 · 일기 · 스킬 · 성격 (갈래는 지웠다 — 아래)
- 기억 목록을 details/summary 로 접음
- 축척을 데이터에서 역산 (가장 빽빽한 쌍 기준) · 배율 1 = 계 전체
- **DB 에 6개월치 목데이터가 들어가 있다** → 아래 「목데이터」 절 참고

### 1단계 완료 — 천체를 하나로 합쳤다 (2026-08-08)

`MemoryBody` 를 없앴다. **갈래 하나가 별 하나**고 기억은 그 별의 성질이다.

| 채널 | 값 |
|---|---|
| 자리 | 분야(방향 조각) × 시작한 지(반경). **방향 문법이 하나뿐이다** |
| 크기 | 붙은 경험 수 = 질량. 심(흰빛)의 지름이 이것이다 |
| 광도 | 남은 기억의 중요도. 알파 + **후광이 번지는 폭**. 안 남았으면 어두운 별 |
| 색 | 분야 색 + trigger 색온도 (`TRIGGER_TEMP` · `tempered` · 최대 34% 혼합) |
| 선 | 중심과 이어짐. `abandoned` 만 끊긴다 |

- 버린 것: `sectorOf` · `threadSectorOf` · `THREAD_BAND` · `ECC_STATUS` ·
  `ECC_TRIGGER` · `TRIGGER_ORDER` · `STAR_SCALE` · `dominantCategory` ·
  `drawOrbit` · `orbitAt` · `speedOf` · 축 두 벌(trigAxis/catAxis) · `layerOf`
- 남은 것: 그리기 · 위성 층 · 확대 카메라 · 컬링 · 축척 역산 · 별 자리 계산
  (`starA`/`starPlane` 은 2단계가 로그 나선으로 대체할 때까지 그대로 쓴다)
- `Elem` 이 궤도 요소를 버리고 `{id, a, plane, lum, size, color, mem}` 만 남았다
- 계 층의 최근 표식이 잔광 → **코로나**(`drawCorona`)로 바뀌었다. 별이 안 도니
  "지나온 길"이 성립하지 않는다. (합치기 전에도 별은 `n=0` 이라 이 표식이
  통째로 죽어 있었다 — 홈의 `latestMemoryIds` 는 아무것도 안 그렸다.)
- `page.tsx` 가 기억을 갈래에 붙여 넘긴다(`ThreadBody.memory`). `moons` 삭제.
  `latestMemoryIds` → `latestThreadIds`
- 순증감: -247줄

**눈으로 확인함** (6개월 시드, 갈래 137 · 그중 92개에 기억):
분야 여덟 방향으로 갈라지고, 밝고 어두운 별이 확실히 갈리고, 코로나가 최근
셋을 짚고, 당기면 위성이 배어 나오고 판독값이 그 별의 것으로 갈린다.

### 1단계 보강 — 크기·광도가 안 읽히던 것 (같은 날)

처음 붙였을 때 **차이가 눈에 안 보였다.** 원인이 둘 다 같았다:
**0 을 기준으로 정규화**하고 있어서 실제 분포가 좁은 구간에 몰리면 눌린다.

실측 분포 (시드 포함):
```
경험 수   1건×49  2건×32  3건×15  4건×14  5건×5 … 15건×1   ← 1~4 에 80%
중요도    1→2 2→4 3→9 4→7 5→9 6→19 7→16 8→11 9→7 10→8    ← 잘 퍼져 있다
```
- 크기 `3.5 + log1p(n)·2` → 1건 4.9 · 2건 5.7. **데이터 대부분이 0.8px 안**
- 광도 `imp/10` → 0.48~1.0. 부드러운 그라디언트에서는 안 갈린다

고친 것:
- **최솟값도 같이 뺀다** (`spanOf`). lo→0, hi→1 로 있는 폭을 다 쓴다.
  폭이 0 이면(전부 같은 값) 0.5 로 줘서 없는 차이를 만들지 않는다
- 크기는 **로그를 먼저 씌우고 그 위에서** 범위를 잰다. 순서가 바뀌면 정규화가
  선형 공간에서 일어나 로그로 누른 뜻이 사라진다
- 광도 정규화에 **기억 없는 갈래를 안 넣는다.** 넣으면 "안 남김"이 최솟값
  자리를 차지해서 실제로 남은 것 중 가장 약한 것과 구분이 안 된다.
  세 층: `LUM_DARK`(0.2) · `LUM_KEPT_MIN`(0.46) · 1
- 광도가 **알파뿐 아니라 후광 폭(bloom)** 도 정한다. 알파만으로는 안 읽혔다 —
  안쪽이 흰빛으로 타는 그라디언트라 심은 어느 밝기에서든 하얗고, 실제로
  갈리는 건 바깥이 어디까지 번지느냐다
- 다만 **정지점을 bloom 으로 되나눠** 심의 px 크기는 radius(질량)만 따르게
  했다. 안 그러면 크기와 밝기가 한 덩어리로 읽혀 **작고 밝은 별과 크고 어두운
  별이 구분이 안 된다**
- 화면에 남은 것이 하나도 없으면(`anyKept`) 층을 안 나누고 전부 제 밝기로
  띄운다. "어둡다"가 "안 남았다"는 뜻이려면 옆에 밝은 것이 있어야 한다 —
  없으면 그건 그냥 화면 전체가 `LUM_DARK` 로 깔리는 것이다

### 1단계 보강 — 남은 것은 '기억'이라고 부른다

판독값이 둘 다 `갈래 ·` 로 시작했다. 천체가 하나뿐이니 맞는 말이긴 한데,
그러면 화면에서 제일 밝은 것들이 무엇인지를 판독값이 답하지 못한다 —
밝기는 "많이 남았다"인데 이름은 여전히 "갈래"였다.

```
기억 있음   기억 · THREAD_COMPLETE · BREAKTHROUGH · ACTIVE · DOCS · 경험 3건 · 날짜
없음        갈래 · ACTIVE · DOCS · 경험 3건 · 날짜
```

남은 이유를 이름 바로 뒤에 전부 붙인다(예전엔 줄 끝의 `· 남음 …` 이었다).
호버(probe)와 펼친 판독값(headline)이 **같은 단어**를 쓴다 — 겨눴을 때와
들어왔을 때가 다른 이름으로 불리면 안 된다.
(한때 갈래 화면만 기억을 안 읽어서 같은 별이 화면마다 다른 이름으로 불렸다.
그 화면은 아래에서 통째로 없앴다.)

### 1단계 마무리 — `/threads` 를 없애고, 위성을 더 늦게 꺼냈다

**갈래 화면(`/threads`)을 통째로 지웠다.** 천체가 하나로 합쳐지면서 홈이 갈래를
전부 그리게 됐고, 그러면 두 화면이 같은 것을 두 번 보여준다. 축척 레일도
다섯 칸(나 · 기억 · 일기 · 스킬 · 성격)이 됐다.

거기에만 있던 것 셋을 홈으로 옮겼다. **하나라도 빠뜨리면 기능이 사라진다:**

| 옮긴 것 | 어디로 |
|---|---|
| **완결 버튼**(`completeThread`) | `map-stage.tsx` → `OrbitalMap.onComplete`. 이게 없으면 갈래를 끝낼 방법이 사이트에 없다 |
| 경험 상한 400 (홈은 220 이었다) | `page.tsx` `MAX_BODIES`. 짧은 쪽에 맞추면 닿던 경험이 안 닿는다 |
| 분야를 교정 반영해 다시 세기(`dominantOf`) | `page.tsx`. 없으면 /diary 에서 분야를 고쳐도 별 색이 안 따라온다 |

`revalidatePath("/threads")` 도 뺐다(`actions.ts`).
기억→별 변환은 `lib/thread-memories.ts` 로 뗐다 — 한때 두 화면이 각자 하다가
같은 별이 '기억' 과 '갈래' 로 다르게 불린 적이 있다.

**위성이 더 늦게 나온다.** `SAT_REVEAL_FROM` 1.45 → **2.6**, `TO` 3.0 → **3.8**.
1.45 는 하필 **별끼리를 견주려고 당기는 구간**이라, 어느 별이 더 밝고 큰지
보려고 조금만 당기면 위성이 벌써 새어 나와 화면이 궤도선으로 덮였다.
이제 zr 1~2.6 은 순수한 별밭이다. 대신 `zoomForStar` 의 하한을 `SAT_REVEAL_TO`
로 올렸다 — 안 그러면 위성이 많은 별은 클릭 착지 배율(2.6)이 반쯤만 드러난
자리라 "눌러서 들어갔는데 안이 비어 보인다"가 된다.

### 1단계 보강 — 별이 제 위성보다 작고 어두웠다

당겨 들어가면 **주인이 손님보다 작았다.** 실측: 경험 1건짜리 어두운 별이
반경 14.5px 인데 제 위성 중 가장 큰 것이 43px. 위계(중심 > 별 > 경험)가
크기로도 밝기로도 뒤집혀서, 확대해 들어간 것이 무엇의 안인지가 사라졌다.

원인은 **두 값이 서로 다른 자에서 나온다**는 것이다 — 별 크기는 질량(경험 수),
위성 크기는 그 경험의 점수. 서로를 모르니 그냥 두면 언젠가 뒤집힌다.
눈으로 맞추지 않고 **불변식으로 못박았다.**

```
크기   별 반지름 ≥ 가장 큰 위성 반지름 × 1.3   (STAR_OVER_SAT)
밝기   별 알파   ≥ 가장 밝은 위성 알파 × 1.22  (SAT_UNDER_STAR 0.82 의 역)
```

- **크기**: `satFloor` — 그 별의 위성들을 실제로 배치한 뒤 가장 큰 것을 재서
  하한을 건다. 그린 반지름끼리 견준다(별 ×3.2 · 위성 ×4.1 로 계수가 달라
  `size` 끼리 직접 비교하면 안 된다). 광도가 후광을 좁히는 몫(`bloomOf`)까지
  되나눠야 실제로 그려질 반지름이 하한을 넘는다.
  위성을 깎지 않고 별을 키운다 — 위성 크기는 "이 근거가 얼마나 셌나"라 깎으면
  뜻이 주는데, 이 배율에서는 옆 별이 `dimOf` 로 지워져 있어 별은 키워도
  잃을 게 없다.
- **밝기**: `starLumOf(el) = lum + (1-lum)·satReveal`. 광도는 별끼리 견주는
  값이라 별밭에서만 뜻이 있다 — 안으로 들어가면 견줄 대상이 제 위성이므로
  1 로 올라간다. 이게 부등식을 **증명한다**(위성 알파 ≤ sysAlpha·dimOf·satReveal,
  별 알파 = sysAlpha·dimOf·(lum + (1-lum)·satReveal) ≥ 그것).
  거기에 위성 알파에 천장 `SAT_UNDER_STAR` 를 씌워 "같다"가 아니라 "더 밝다"로
  만들었다. 겨눈 것(lit)은 예외 — 그건 광도가 아니라 표식이라 채널이 다르다.

최악의 경우로 검산했다(경험 1건 · 기억 없음, 배율 2.8~6.5): 어디서도
크기 1.30배 · 밝기 1.22배 아래로 안 내려간다.

### 2~5단계 완료 — 우주로 다시 짰다 (2026-08-08)

**분야가 방향에서 은하가 됐다.** 조각(SECTOR)도 축(drawAxes)도 별자리 선도
사라졌다. 층이 넷이다.

```
0  우주    은하 여덟이 점으로 · 코스믹 웹 필라멘트가 잇는다
1  은하    원반(진행 중 · 나선팔 2개 · 안쪽이 최근) + halo(완결 안쪽 · 방치 바깥)
2  항성계  별 하나 + 위성(경험)
3  행성    경험 하나의 판독값
```

| 단계 | 한 것 |
|---|---|
| 2 | 로그 나선 `r = BULGE·e^(bθ)` · 피치각 17도 · 0.91 바퀴 · 팔 2개 |
| 3 | halo 분리 — `HALO_DONE`(1.08~1.32) · `HALO_LEFT`(1.44~1.78) |
| 4 | 층 배율을 크기에서 유도 — 손으로 맞춘 문턱이 없다 |
| 5 | `GALAXY_SITES` — 손으로 놓은 코스믹 웹 표 · 최근접 이웃으로 필라멘트 |

**왜 40도 조각으로는 안 됐나**: 로그 나선의 힘은 각도를 많이 쓴다는 데 있는데
(원리 3) 조각 안에서는 감을 각도가 없다. 분야를 '어느 쪽'이 아니라 '어디'로
옮겨야 온 원이 열린다. 그래서 2단계는 5단계 없이는 성립하지 않는다.

**좌표가 극좌표에서 직교좌표로 바뀌었다.** 중심이 여럿이면 `a`·`plane` 로는
표현이 안 된다. `Elem` 이 `{x, y, ...}` 가 됐고 `orbitPoint` 도 따라 바뀌었다.

**축척 역산이 통째로 사라졌다** (`unitNeed` · `PACK_MARGIN` · `PACK_MAX`).
그건 겹침을 사후에 미는 일이었는데, 배치가 원리 2(힐 구면)를 지키면 애초에
안 겹친다. 덤으로 그 역산의 병도 없어졌다 — 우연히 포개진 쌍 하나가 축척을
수만 px/단위로 튀게 하던 것.

#### 실수했다가 고친 것 — 층 배율

`R`, `R²` 으로 문턱을 잡았다가 틀렸다. **원리 2 는 간격에 대한 규칙**이라
자식 반경은 부모 *간격*의 1/R 이지 부모 *크기*의 1/R 이 아니다. 코스믹 웹은
은하 간격(1.2)보다 훨씬 넓게 퍼져 있어(반경 3.2) 우주/은하 비가 30 이 아니라
45 다. 그 어긋남이 곱해져서, `R²`(900배)까지 당겨도 항성계가 별 후광 속에
파묻혀 있었다 — 실제로는 6600배가 그 자리였다.

크기에서 직접 푼다: 반경 s 인 것은 `zr = FILL·maxR/s` 에서 화면을 채운다.

```
우주 반경 3.22 · 은하 반경 0.071 · 항성계 반경 0.00039
→ 은하 층 zr 36 · 항성계 층 zr 6596
휠 한 칸 1.45배 기준 — 우주→은하 10칸 · 은하→항성계 14칸
(누르면 한 층씩 바로 내려간다. 휠은 그 사이를 훑는 용도다)
```

#### 나이를 은하 안에서 잰다

절대 경과일(`radiusOf`)을 버렸다. 은하마다 시간대가 다르다 — 4년 된 분야와
이번 주에 시작한 분야에 같은 자를 대면 한쪽은 가장자리에, 다른 쪽은 중심에
뭉친다. **원반과 halo 도 각자 제 자를 쓴다**: 진행 중인 것은 대개 최근이라
한 자로 재면 원반 별이 전부 팽대부에 몰려 나선이 감기다 만다(실측: DEV 은하
37개 중 진행 중 12개가 전부 안쪽 1/3 에 들어갔다).

잃은 것: 은하끼리 나이를 견줄 수 없다. 얻은 것: 어느 은하든 안이 고르게 찬다.

#### 나선팔을 선으로도 긋는다

팔 위의 별만으로는 안 읽힌다. 진행 중인 갈래는 원래 소수라(DEV 37개 중 12개)
팔 하나에 예닐곱이고, halo 스물다섯이 감싸면 곡선이 묻힌다. **예전 축 점선이
하던 일을 팔이 이어받는다** — 배치와 같은 식(`ARM_SPAN`)을 쓰므로 별은 반드시
그 선 위에 얹힌다.

#### 눈으로 확인한 것 / 못 한 것

- 확인: 우주 층(은하 + 필라멘트) · 은하 층(나선팔 2개에 별이 얹힘 · halo)
  · 은하를 누르면 한 층 내려감 · 판독값·범례 갱신 · 확대/축소 앵커
- **못 함**: 항성계 층까지 내려간 화면. 수치로만 맞췄다 — 사람이 봐야 한다.

---

## 카메라를 앵커로 다시 짰다 (2026-08-08)

"내가 원하는 쪽으로 확대가 안 된다"는 지적에서 시작해 **버그 둘**을 찾았다.
둘은 별개이고, 앞의 것만 고쳐서는 안 나았다.

### 1. 드래그가 확대 목표를 덮어썼다

```
offX = offXTarget = clamp(offX + dx)     // ← 목표를 현재값으로 갈아엎는다
```

휠은 "커서 밑 점을 붙든다"를 목표 공간에서 풀어 `offXTarget` 에 넣는데,
확대가 이징되는 동안 마우스가 조금만 움직이면 그 목표가 **이징 중이던 값**으로
덮어써졌다. 관성 블록도 매 프레임 `offXTarget = offX` 를 했고, 휠 핸들러의
"관성을 먼저 끈다"는 그걸 막는 반창고였다.

### 2. (진짜 원인) 앵커가 도착점에서만 맞았다

화면 자리는 `w/2 + offX + 세계x·baseUnit·zoom` 처럼 **offX 와 zoom 이 곱해져서**
나온다. 둘을 따로 선형 이징하면 중간 프레임은 어느 앵커도 만족하지 않는다 —
가는 내내 대상이 미끄러지다 마지막에 제자리를 찾는다. 실측으로 6칸 확대 뒤
겨눈 은하가 커서에서 **69px** 벗어나 있었다.

### 고친 방식 — 카메라 = 배율 + 앵커

`offX` 를 상태에서 뺐다. 카메라는 이제 약속 하나다:
**"세계점 anchorW 는 화면점 anchorS 에 있다."** 이동량은 매 프레임 유도된다.

```
cx = anchorSX - anchorWX · unit          (applyCamera)
```

배율이 어떤 중간값이든 이 식이 앵커를 지키므로 **이징 내내** 안 미끄러진다.

| 손짓 | 하는 일 |
|---|---|
| 휠 | 앵커를 커서에 **못박는다**(현재=목표, 이징 없음). 배율만 이징 |
| 드래그 | 앵커의 **화면점**을 민다. 배율 목표는 안 건드린다 |
| 클릭 | 앵커 **목표**를 (대상, 화면 가운데)로 — 앵커도 이징해 미끄러져 온다 |

곁들여 고친 것:
- **배율을 로그 공간에서 이징한다.** 층 폭이 수천 배라 선형이면 안쪽에서
  기어가고 바깥에서 날아간다 — 같은 한 칸이 어디서 굴렸느냐에 따라 달라진다.
- **벽에 걸리면 앵커를 그 자리로 다시 잡는다.** 좌표만 자르면 다음 프레임에
  같은 앵커가 또 벽 밖을 가리켜 부르르 떤다.
- **창 크기가 바뀌어도 안 잃는다.** 예전엔 이동량을 0 으로 되돌렸는데, 수천
  배까지 들어갈 수 있게 된 지금은 그 배율 그대로 우주 원점으로 내던져졌다.
  앵커는 세계 좌표라 창과 무관하다 — 화면점만 같은 비율 자리로 옮긴다.

검산: 이징 240프레임 내내(드래그를 섞어도) 커서 밑 점이 **0.000000px** 이동.

### 이어서 잡은 카메라 버그 둘

**3. 위쪽 은하로는 확대가 튕겼고 드래그로도 못 갔다.**
`offLimit` 이 세로 한계에 `FLATTEN`(0.46)을 곱하고 있었다. 그건 계가 원점
둘레의 납작한 원반 하나였을 때 값인데, **은하 자리(`GALAXY_SITES`)의 y 는
안 눌린 생값**이다 — 눌리는 건 은하 *안*의 원반뿐이다(starXY). 그래서 세로가
실제보다 좁게 잡혔다.

```
세로 한계  maxR × FLATTEN = 1.05
ETC 자리   y = -1.34        ← 한계 밖. zr 9 부터 벽에 걸린다
COMMUNITY  y = -0.94        ← 한계 안. 그래서 ETC 쪽만 그랬다
```

벽에 걸리면 `applyCamera` 가 앵커를 다시 잡으므로 화면이 위로 튕겼다.
한계를 **축마다 제 폭으로**(`worldMaxX` · `worldMaxY`) 잰다. 은하 *안*의
세로 폭에만 FLATTEN 을 먹인다.

**4. 누르면 두 동작으로 쪼개져 보였다.**
"권도형과 그 분야를 잇는 선 가운데로 확 확대됐다가, 그다음 그 분야로 이동."
원인: 세계점(anchorW)을 이징했는데 **배율은 로그로, 세계점은 선형으로** 가서
배율이 훨씬 빨리 자랐다 — 중간 프레임에는 아직 원점 근처인 채로 깊이 들어간다.

대상을 **처음부터 앵커로 못박고 그 화면점만 가운데로 미끄러뜨린다.** 대상은
한 순간도 화면에서 안 벗어나고, 확대와 이동이 한 동작이 된다. 화면점 이징이라
눈에 보이는 속도도 고르다(세계점 이징은 배율 따라 화면 속도가 수천 배 널뛴다).
물러나기(`resetCamera`)도 같은 규칙 — 원점을 못박고 가운데로.

```
ETC 클릭 시 대상이 화면 중심에서 벗어난 최대 거리
  옛 방식  971px  (한 프레임 최대 87px — 튀어 보인다)
  지금     300px  (시작 거리에서 단조 감소 · 한 프레임 최대 31px)
```

---

## 나선팔을 빛줄기로 · 최근 표식을 우주 층까지 (2026-08-08)

### 나선팔 — 1px 선 → 겹친 점

"그냥 선인데" 라는 지적. 실제 나선팔은 가스와 먼지가 깔린 길이지 그어놓은
획이 아니다. `trailDot`(잔광이 이미 쓰는 어휘)을 겹쳐 찍어 띠로 만든다 —
새 모양을 안 만든다.

- 두께는 반경을 따라 조금씩 벌어지고, 세기는 반대로 사그라든다.
  끝에서 0 이어야 잘린 자국이 안 남는다 — 팔은 끝나는 게 아니라 옅어져 사라진다.
- **점 개수를 호 길이로 잰다.** 처음엔 반경 폭으로 쟀는데 로그 나선의 호
  길이는 그 `√(1+b²)/b` 배 — 지금 값으로 **3.5배**다. 그만큼 성기게 찍혀서
  점선으로 보였다. 거기에 직전 점까지의 거리만큼 반지름 하한을 둬 반드시
  겹치게 한다(나선은 안쪽이 촘촘하고 바깥이 성겨 한 값으론 못 맞춘다).
- 두께는 두 번 깎았다(0.55 → 0.13 → 0.07). 별을 흩뿌린 폭(`ARM_WIDTH`)에
  맞췄더니 팔이 아니라 뭉갠 덩어리가 됐다 — **별은 팔 둘레에 흩어지는 것이지
  팔이 그 폭을 다 채우는 게 아니다.** 부드러움은 두께가 아니라 겹친 점이 만든다.

### 은하마다 제 기울기 (`spinOf`)

없으면 여덟 은하가 똑같은 자세로 서 있다 — 같은 그림을 복사해 붙인 것처럼
보이고, 그게 "자연스럽지 않다"의 나머지 절반이었다.

**배치와 그리기가 `armAt` 하나를 같이 쓴다.** 갈리면 별이 팔에서 벗어나고,
그러면 팔은 아무 데도 안 지나는 선이 되고 별은 왜 거기 있는지 모를 점이 된다.

### 최근 표식이 우주 층까지 올라갔다

코로나(고리)가 별에만 있었다. 지도를 열면 은하 몇 개만 보이는데 **어디서
방금 일이 있었는지 알 길이 없었다** — 아래층까지 내려가야 비로소 보였다.
이 파일에 이미 박혀 있던 원칙("같은 표식이 층을 건너 읽힌다")의 빠진 칸이다.

```
우주    방금 뭔가 들어온 은하   ← 새로 넣음
은하    그 별(갈래)
항성계  그 경험 — 실제로 도니까 잔광이 대신한다
```

- 왜 고리인가: 색은 분야, 크기는 질량, 광도는 중요도로 이미 다 찼다. 고리는
  아무 데도 안 쓰이는 **유일하게 빈 채널**이다. 맥동은 안 쓴다 — 화면에서
  유일하게 뜻 없이 움직이는 것이 되어 뭘 보려 해도 눈이 끌려간다(1단계에서
  이미 버린 방법).
- 은하의 순위는 그 안에서 **가장 최근인 것**을 따른다. 갈래 셋이 한 은하에
  몰리면 그 은하가 1등이지 3등이 아니다.
- **별이 드러나면 은하 고리는 사라진다.** 안 그러면 은하만 한 거대한 원이
  되어 표식이 아니라 노이즈다 — 그 층의 주인은 이미 별의 코로나다.
- 잦아든 뒤 남는 세기를 0.32 → 0.5 로 올렸다. 예전엔 몇 초 지나면 있는지도
  몰랐다. 그리고 고리 반지름을 **그려진 반지름**에서 받는다(별과 은하는
  반지름 셈법이 달라 size 로는 못 맞춘다).

### 판독값(호버 툴팁)을 라벨 붙은 표로

한 줄에 전부 `·` 로 이어 붙어 있었다:

```
기억 · THREAD_COMPLETE · BREAKTHROUGH · ACTIVE · DOCS · 경험 3건 · 2026.06.16 시작
```

값이 죄다 같은 모양의 대문자 토큰이라 **어느 게 뭔지 분간이 안 됐다** —
THREAD_COMPLETE 와 ACTIVE 와 DOCS 가 나란히 있는데 셋 다 다른 종류다.

```
기억
Army Sim 프로젝트 상태 확인
[Claude] [ChatGPT]
─────────────────────
남은 이유   NEW_SKILL
분야        DEV
상태        ACTIVE
경험        2건
시작        2026.08.05
```

- `probe` 가 문자열 하나(`sub`) 대신 `kind` + `rows[{k,v}]` 를 든다.
  무엇인가 → 이름 → 값들 순으로 위에서 아래로 좁혀 읽는다.
- **값은 enum 그대로 둔다.** 한글 대응표를 두면 화면과 DB·프롬프트가 다른
  어휘를 쓰게 되고 값이 늘 때마다 번역을 빠뜨린다(`tag` 주석의 규칙).
  한글은 **라벨**에만 — 그건 값이 아니라 "이 자리가 무엇인가"다.
- 라벨 열이 한 줄로 서니 눈이 세로로 훑고, 값 열도 자리가 고정된다.
- 스킬 칩을 값들 **위로** 올렸다. "왜 남았나"에 답하는 것이라 나머지
  판독값보다 먼저 읽혀야 한다.
- 완결된 것은 `시작`·`완결` 두 줄이 아니라 `기간  A → B` 한 줄이다.
- 네 종류 모두 정리했다: 은하 · 기억/갈래 · 경험 · 축.
- **날짜·분야가 맨 위다.** "언제, 무슨 갈래냐"가 먼저 잡히고 나머지는 그 뒤에
  딸리는 값이다. 경험 판독값도 같은 순서라 층을 오가며 같은 자리를 읽는다.

### 화면 어휘 정리

- **`halo` 를 화면에 안 쓴다.** 코드 용어가 그대로 새어 나갔었다
  ("진행 중 4건 · 나머지 0건은 halo"). 범례는 이미 한국어로 말하고 있는데
  거기만 다른 어휘였다. 은하 판독값을 상태별 줄로 풀었다.
- **`남은 것` → `기억`.** 우하단 '기억 5' 와 같은 값을 다른 이름으로 부르고
  있었다 — 한 화면에 두 어휘를 두지 않는다.
- **`놓음` → `잊혀짐`** (status 는 `abandoned` 그대로). 사람이 놓은 게 아니라
  손이 안 가서 밀려난 것이라(야간 배치가 30일 무활동으로 넘긴다) 그쪽이
  실제에 가깝다. 범례도 같이 고쳤다.
  - **주의**: 모델에는 `memories.forgottenAt`(Lv.80 망각)이 따로 있다. 화면에는
    안 나온다(잊힌 기억은 `liveMemories` 에서 빠진다) — 지금은 안 부딪히지만
    그 기능을 화면에 올리면 어휘가 겹친다.
- 은하 판독값에서 `진행 중` 을 뺐다. 갈래에서 나머지 둘을 뺀 값이라 화면에
  이미 있는 정보고, 나선팔 안이냐 밖이냐로 더 잘 읽힌다.

### 갈래의 분야는 어디서 정해지나 (물어봐서 확인한 것)

**두 군데에서 각각 센다. 중복이 아니라 세는 대상이 다르다.**

| | 세는 것 | 언제 |
|---|---|---|
| DB | `experiences.category` — 모델 판정(inferred) | 경험이 붙을 때(`attach`)마다 SQL 로 |
| 화면 | 그 위에 사람 교정을 겹친 값(declared) | 렌더할 때 `dominantOf` |

엔진은 corrections 를 안 본다(세션 처리 시점에는 교정이 아직 없다). 그래서
`/diary` 에서 고친 것이 별에 바로 반영되려면 화면이 다시 세야 한다.
동률이면 양쪽 다 이름순 — 렌더마다 색이 바뀌면 안 된다.

**분야가 은하가 되면서 생긴 것**: 경험 하나의 분야를 고쳐 최빈값이 뒤집히면
그 갈래가 **통째로 다른 은하로 이사한다.** 예전엔 색만 바뀌던 자리다.
의도된 동작이지만("지금까지 무엇을 한 작업인가"가 갈래 분야의 정의) 알고 있을 것.

### 은하를 색 묶음이 아니라 **카테고리**로 (2026-08-08)

`search` 갈래가 `ETC` 은하에 들어가 있었다 — 왜 거기 있는지 화면 어디에도
답이 없었다. 5단계에서 색 묶음(여덟)을 그대로 은하로 물려받은 탓이다.

**묶은 이유는 순전히 색이었다.** "검은 배경 위 작은 후광으로 구분되는 색은
여덟이 한계다"(CAT_GROUP_DEFS 주석). **자리는 그 한계를 안 받는다** — 우주에
점 열넷을 놓는 건 아무 문제가 없다. 그때 이 제약을 다시 따졌어야 했다.

```
                 예전(9)              지금(14)
etc     ← search · etc          SEARCH · ETC 가 각자
life    ← news · finance ·      NEWS · FINANCE · SHOPPING ·
          shopping · productivity  PRODUCTIVITY 가 각자 (색은 넷 다 자홍)
media   ← entertainment · music  ENTERTAINMENT · MUSIC 이 각자 (색은 둘 다 녹색)
```

두 채널이 이제 **서로 다른 것**을 말한다:

| 채널 | 말하는 것 | 개수 |
|---|---|---|
| 색 | 그 결이 무엇인가 — 같은 색이면 비슷한 종류 | 8 (`CAT_GROUPS`) |
| 자리·이름 | 무슨 일인가 | 14 (`GALAXY_SITES`) |

같은 자홍색 넷이 흩어져 떠 있어도 안 헷갈린다. 오히려 "이것들이 한 결이구나"가
색으로 읽히고 각각이 무엇인지는 이름이 말한다.

- `GALAXY_SITES` 를 14개로 다시 깔았다. 필라멘트 둘 + 마디 밖 셋.
- **컴파일 타임 검사**를 붙였다(`_sitesCoverCategories`). 개수가 어긋나면
  마지막 자리로 조용히 떨어져 두 은하가 포개지는데, 런타임엔 아무 티도 안 난다.
  `as const satisfies` 로 써야 길이가 리터럴로 잡힌다 — 타입 주석을 달면
  `length` 가 `number` 가 되어 검사가 통째로 무력해진다(실제로 한 번 걸렸다).
- `CAT_GROUPS` 는 이제 **색 전용**이다. 범례가 계속 쓴다.
- 검산: 열넷 최소 간격 1.02 vs 은하 지름 0.14 → 어느 둘도 안 겹친다.
  `maxR` 2.28 → 3.66 으로 넓어졌고 층 배율은 거기서 자동으로 따라왔다.

**기억에는 `category` 컬럼이 없다.** 그래서 갱신될 것도 없다 — 갈래의 분야를
그대로 쓴다. 기억이 별개 천체였을 때는 `dominantCategory` 가 그 기억의 근거
경험들로 색을 따로 정했는데, 1단계에서 천체를 합치며 그 함수를 버렸다.

### 같이 고친 위계 오류 둘

1. **은하가 제 별을 삼켰다.** 은하 판정 반경이 원반을 따라 자라는데 `nearest`
   가 그걸 CAPTURE(2.4)배로 또 넓혀서, zr 27 에서 은하 하나가 반경 **417px** 를
   먹었다 — 그 안의 별을 누르면 은하가 잡혀 도로 밖으로 튕겼다.
   문턱 `STAR_HIT` 하나를 양쪽이 반대로 써서 배타적으로 만들고, 판정 반경도
   그려진 점에 맞춰 상한을 뒀다.
2. **겨눈 위성이 제 별보다 컸다.** `lit` 이 위성을 1.6배로 키우는데 별의 크기
   하한이 그걸 몰랐다(69.5px 대 56.5px). 하한이 `SAT_LIT` 을 감안한다 —
   호버 여부로 하한을 바꾸면 겨눌 때마다 별이 펄쩍 뛰므로 **항상** 감안한다.

전수 검사(배율 400단계 × 광도 3 × 질량 2): 크기 1.30배 · 밝기 1.22배 아래로
안 내려간다.

#### 실제로 버린 것

`MemoryBody` · `sectorOf` · `threadSectorOf` · `SECTOR` · `starA` · `starPlane` ·
`STAR_BASE/SEP/SLOTS/FILL/RADIAL_JITTER` · `radiusOf` · `unitNeed` ·
`PACK_MARGIN` · `PACK_MAX` · `UNIT_FRAC` · `satFieldF` · `THREAD_BAND` ·
`ECC_STATUS` · `ECC_TRIGGER` · `TRIGGER_ORDER` · 계 층의 `drawAxes` · 별자리 선.

살아남은 것: 그리기(그라디언트 · 코로나 · 잔광 · 판독값 · 확대 카메라 · 컬링).

### 남은 것 · 지켜볼 것

1. **`LAYER_RATIO` 는 손잡이 하나다.** 30 이 휑하면 낮추면 전부 따라온다
   (은하 반경 · 항성계 반경 · 층 배율 · 휠 폭).
2. **halo 가 원반을 압도한다.** 실측 137개 중 진행 중이 35개뿐이라 어느 은하든
   halo 가 2/3 다. 사실이긴 한데 "나선은하"보다 "성단"에 가깝게 보인다.
   완결을 원반 바깥쪽에 남기는 안도 있다 — 지금은 계획서대로 halo 로 보냈다.
3. **`경험 0건`짜리 별.** 상한(400) 밖의 갈래다 —「안 끝난 것 2」.
4. 은하가 없는 분야(갈래 0건)는 자리도 안 잡는다. `GALAXY_SITES` 에 빈칸이
   생기지만 필라멘트는 있는 것끼리만 잇는다.

---

## 목데이터 (시드) — 지금 DB 에 들어가 있다

> **"목데이터 지워줘" 라는 말을 들으면 이 절만 보면 된다.**

2026-08-08, 지도가 데이터 많을 때 어떻게 보이는지 확인하려고 넣었다.
**실데이터와 한 DB 에 섞여 있다.** 지금 화면에 보이는 갈래·기억·경험 대부분이
이것이다 (실데이터는 경험 21 · 갈래 5 · 기억 4 수준).

```
넣은 것   세션·경험 405 · 갈래 131 · 기억 87   (180일치)
표식      모든 id 가 '5eed' 로 시작한다
```

### 지우기

```bash
cd apps/web
npx tsx --tsconfig scripts/tsconfig.json scripts/seed-6months.mts --clear
```

지운 개수를 줄별로 찍는다. FK 순서대로 지우고, **시드 갈래를 가리키는
앱 생성 기억까지 같이** 지운다 — 그게 없으면 갈래 삭제가 FK 에 막혀 통째로
실패한다(실제로 한 번 막혔다).

### 남았는지 확인

```sql
select
  (select count(*) from sessions    where id::text like '5eed%') as sessions,
  (select count(*) from threads     where id::text like '5eed%') as threads,
  (select count(*) from experiences where id::text like '5eed%') as experiences,
  (select count(*) from memories    where id::text like '5eed%') as memories;
```

전부 0 이면 깨끗하다.

### 다시 넣기

```bash
npx tsx --tsconfig scripts/tsconfig.json scripts/seed-6months.mts          # 미리보기
npx tsx --tsconfig scripts/tsconfig.json scripts/seed-6months.mts --apply  # 실행
npx tsx --tsconfig scripts/tsconfig.json scripts/seed-6months.mts --years  # 1~4년 전 (연 단위 확인용)
```

고정 시드 PRNG 라 몇 번 돌려도 **같은 데이터**가 나온다 — "어제 본 화면"과
비교할 수 있다.

### 안 건드리는 것

- **`user_skills`** — PK 가 `(user_id, skill_name)` 이라 id 표식을 붙일 수가
  없다. 넣으면 진짜 스킬과 섞여 되돌릴 수 없으므로 아예 안 넣는다.
  대신 `experience_skills` 만 넣어서 호버 칩은 뜨고 `/skills` 는 안 더럽힌다.
- **`characters` 의 누적 카운터**(experience_count 등) — 넣을 때도 지울 때도
  안 만진다. 그래서 화면 우하단 숫자(경험 21 · 스킬 12)는 시드와 무관하게
  실데이터 그대로다. **지운 뒤 따로 손볼 것이 없다.**

---

## 지금 상태

`map/cosmos` 브랜치. **원격에 안 올라갔다** — 배포도 안 했다.

```
DB (Supabase)   시드 없음(지웠다). 세션 23 · 경험 29 · 갈래 7 · 기억 3 · 스킬 14
                교정 6 · 질문 2 · 일기 3일치(죽은 id 없음)
프롬프트         v8  (골든셋 14/17 — 기준선과 같다. success·partial·thread new 는 상시 실패)
```

---

## 진행 중 갈래 후보에 요약이 안 붙고 있었다 (2026-08-08 발견·수정)

"오늘 생긴 경험이 왜 메타버스 행사 기억에 붙었나"를 따라가다 찾았다.

### 무슨 일이 있었나

갈래 `kt cloud TECH UP 2기 메타버스 행사 참여`(7d64d125):

```
08-05 23:54  etc  M90  kt cloud TECH UP 2기 메타버스 행사 참여 · ZEP…   ← 진짜 그 행사
08-06 01:01  etc  M 0  kt cloud … 팀 협업 문서와 디자인
08-06 23:52  dev  M90  ZEP 메타버스 행사 참여 중 … Project NA와 …       ← 다리
08-07 03:45  dev  M90  Project NA와 SOLDIER: A DAY … 기능 구현          ← 오늘
08-07 08:15  dev  M 0  Project NA 로컬 서버 개발 · 프롬프트 캐싱
08-07 10:10  dev  M 0  Project NA 개발 · 코스믹 웹 조사
```

오늘 것 셋은 전부 Project NA 개발인데 메타버스 갈래에 붙었다. 모델이 세 번 다
`attach` 를 골랐다(llm_outputs 확인). 그리고 갈래 category 가 `etc` → `dev` 로
뒤집혔다 — 붙을 때마다 최빈값으로 다시 세니까(attach UPDATE). **제목만 원래
것으로 남았다.**

### 진짜 원인 — 183분 세션이 안 쪼개졌다

**처음엔 후보 목록 탓으로 봤는데, 세션 원본까지 파보니 그게 아니었다.**
(아래 「곁가지로 찾은 진짜 버그」는 따로 고쳤다 — 이 건의 원인은 아니다.)

체류 시간을 보면 분명하다:

```
08-06 23:52  183분  zep.us 22분 · claude 12 · 대한항공 11 · …   ← 진짜 섞인 세션
08-07 03:45  241분  localhost 201분 · supabase 26 · zep.us 3분   ← ZEP 1.2%
08-07 08:15   47분  localhost 24 · chatgpt 10                     ← ZEP 없음
08-07 10:10   62분  localhost 52 · meshy 5                        ← ZEP 없음
```

그리고 `llm_outputs` 를 보면 183분 세션에서 모델이 **경험 1개**를 냈고
`segment_ids: [0..18]` — **19개 구간을 전부 하나에 몰아넣었다.** 코드가 합친
게 아니라 모델이 안 쪼갠 것이다. (같은 날 241분 세션은 2개로 잘 쪼갰다.)

사슬:

```
① 183분 세션이 안 쪼개짐            ← 뿌리
     ZEP + Claude API + 대한항공 쇼핑 + Project NA + SOLDIER 를 한 경험으로
② 그 경험이 메타버스 갈래에 붙음      ← ZEP 이 최대 체류라 이건 맞는 판단
③ 그 순간 갈래가 오염된다
     category    etc → dev        (붙을 때마다 최빈값 재계산)
     마지막 요약  "… Project NA 와 SOLDIER …"
④ 이후 Project NA 세션들이 계속 붙음  ← 신호가 다 그렇게 가리킨다
```

**모델이 틀린 게 아니라 데이터가 틀렸다.** 모델이 받는 신호 셋 중 둘이 ③에서
오염됐고, 남은 하나(제목)는 **처음 값에 박제**돼 있다 — 갈래 제목은 어디서도
갱신되지 않는다. 그래서 "제목은 메타버스, 분야는 dev, 내용은 Project NA" 인
갈래가 생기고, 화면에도 그 제목으로 뜬다.

### 곁가지로 찾은 진짜 버그 (고침) — 진행 중 갈래에 요약이 안 붙었다

`buildActiveThreadsList` 는 `최근: {마지막 요약}` 을 그리게 돼 있고 주석까지
있다:

> 제목만 주면 그 작업이 무엇을 하던 것인지 알 수 없어, 분야만 같아도 붙이게
> 된다. 마지막으로 한 일을 함께 줘야 "이번 세션이 그 다음 단계인가"를 볼 수 있다.

**그런데 `activeThreadRows` 쿼리가 `lastSummary` 를 안 뽑았다.** 늘 `undefined`
라 그 줄이 한 번도 안 그려졌다. 잠긴 갈래(`findDormantCandidates`)는 제대로
채우고 있었으니 **흔한 쪽(진행 중)만 헐벗은 채로** 돌았다. 고쳤다.

**다만 이 건은 이걸로 안 고쳐진다 — 오히려 반대로 갈 수 있다:**

```
1. "kt cloud … 메타버스 행사 참여"  최근: Project NA와 SOLDIER … 로컬 서버에서  ← 더 그럴듯
2. "Project NA 기술 아키텍처 설계"  최근: … 설계를 마무리하고                    ← 끝난 것처럼
```

`최근` 은 갈래의 **마지막 상태**만 말하는데, 이 건은 그 마지막 상태가 이미
오염된 것이 문제다. 그래도 고친 이유는 주석과 쿼리가 어긋난 건 그 자체로
버그이고, 잠긴 갈래는 이미 받고 있던 것이라서다.

#### 왜 못 쪼갰나 — 합계가 두 목록에 걸쳐 있다

프롬프트는 이렇게 시킨다: **"기본은 나누지 않는 것이다. 애매하면 하나로 두고
부수적인 것은 detail 에 한 줄로 적는다."** 모델은 시킨 대로 했다.

그런데 이 세션은 애매하지 않았다. 대상별 합계를 내보면:

```
대상          earlier  segments   합계
ZEP 행사         46분     22분     68분
Project NA      40분      6분     46분   ← 명백한 제2 대상
Claude API       2분     16분     18분
대한항공 쇼핑     0분     17분     17분
```

**Project NA 가 46분이다.** 쪼갰다면 그게 제 갈래로 갔을 것이고 메타버스
갈래는 ZEP 만 받아 오염이 없었다.

모델이 이걸 알려면 **모양이 다른 두 목록에 걸친 산수**를 해야 한다:

```
earlier.top: ["zep.us · … 46분", "localhost · Project NA 37분", …]  ← 대상별 합계, 순서 없음
segments:    [0]2분 [1]2분 [2]6분 …                                 ← 개별 구간, 순서 있음
```

LLM 이 특히 못하는 종류의 일이다. 프롬프트도 earlier 를 "앞부분이니 빼먹지
마라" 라고 경고할 뿐 합계를 주지는 않는다. **문턱(10분)도 안 알려준다** —
`MIN_SPLIT_SEC` 은 코드에만 있다. 그래서 보수성이 두 겹으로 걸린다:
코드가 이미 10분 미만을 흡수해 과분할을 막는데, 모델은 그걸 모르니 또 사린다.

(구간이 나뉘는 기준은 **연속 체류**다. 같은 대상도 나갔다 오면 새 구간이라
ZEP 이 2·6·14분 세 줄로 나온다. 대상이 아니라 시간대로 자른 것이다.
1분 미만은 `via` 로 접히고, 상한 20칸을 넘긴 앞부분은 `earlier` 로 접힌다.)

### 안 고친 것 — 어디를 손봐야 하나 (근거 부족, 표본 1건)

1. **분할** (뿌리). 「안 끝난 것 6」이 "분할이 **과하게** 나오는지 지켜볼 것"
   이라고 적어뒀는데 실제로는 **모자란** 사례가 나왔다. 손볼 곳 둘:
   - ~~**대상별 합계를 프롬프트에 준다.**~~ **넣었다** (아래 절)
   - **10분 문턱을 알려준다.** "작은 것은 코드가 흡수하니 대상이 다르면 일단
     나눠라". 안전망이 이미 있는데 모델이 몰라 이중으로 사리는 상태를 푼다.
     이 둘은 짝이다 — 문턱을 알아도 합계를 모르면 못 쓴다.
   - 프롬프트를 만지므로 **eval 없이 올리지 않는다**(카테고리 재판정이 프롬프트
     한 줄로 무너진 적이 있다). 지금 골든셋 14/17.

   ※ **도메인별로 합치는 것은 안 된다**(합계를 *더해* 주는 것과 다르다):
     ① 같은 도메인이 같은 대상이 아니다 — `localhost · Project NA` 와
        `localhost · SOLDIER` 는 다른 일이다(프롬프트가 명시). 지금은
        도메인+제목(`viaLabel`)으로 묶는다.
     ② 순서가 정보다 — 뒤섞였다는 사실이 "행사 중 딴짓"과 "A 하고 B"를 가른다.
     ③ 시각을 잃는다 — 쪼갠 경험의 `occurred_at`·`duration_min` 이 배정 구간에서
        나온다.
2. **갈래 제목이 안 바뀐다.** category 는 최빈값으로 다시 세는데 title 은 처음
   값 그대로다. 후보 ① 밤 배치가 제목도 다시 짓는다(요약 LLM 을 이미 부르므로
   얹기 쉽다) ② 주제가 크게 벌어지면 갈래를 쪼갠다 ③ 사람이 고칠 장치를 준다.
3. **후보 목록에 갈래의 '처음'도 준다.** 지금은 마지막만 준다. 처음+마지막이면
   모델이 주제가 옮아간 것을 볼 수 있다 — 다만 이 사례에선 둘 다 메타버스를
   언급해서 안 갈렸을 수도 있다.

**표본 1건이다.** 같은 일이 몇 번 더 나는지 보고 정한다.

---

## v8 로 전체 재구축 (2026-08-08 · 실행 완료)

`PROMPT_VERSION` 8 · `SYSTEM_PROMPT_V7` → `V8` (v6→v7 때와 같이 이름도 같이 올린다).
세션 23건을 시간순으로 다시 태웠다. **23/23 성공.**

```
           경험  갈래  기억  스킬  교정  질문
재구축 전    24     6     5    12     6     2
재구축 후    24     4     4    13     6     2
```

### 원래 문제가 풀렸다

```
전  "kt cloud TECH UP 2기 메타버스 행사 참여" · dev · 경험 6건   ← Project NA 가 섞여 있었다
후  "KT Cloud TECH UP 2기 행사 참여" · community · 경험 2건      ← 행사만 남았다
    "Project NA 개발" · dev · 경험 19건                           ← 제 갈래로 갔다
```

category 도 `dev` → `community` 로 제자리를 찾았다. 갈래가 6 → 4 로 준 것은
같은 일이 흩어져 있던 것이 하나로 모였다는 뜻이다(Project NA 12+6 → 19).

### 검증

- **교정 6건 전부 살아 있고 의미가 맞는 경험에 붙었다.** 고아 0건.
  (`stuck→partial` 이 "Supabase 오가며" 경험에, `etc→dev` 가 KT Cloud 경험에.)
- **일기 3일치 재생성 완료 · 죽은 id 0건.** 안 했으면 `/diary` 근거가 통째로
  사라졌다 — HANDOFF 에 "실제로 세 번 빠뜨렸다" 고 적혀 있던 그것.
- 백업: `/tmp/na-rebuild-backup-1786113594364.json`

### 분할이 왜 안 됐나 — 프롬프트가 아니라 네 가지 결함이었다

첫 재구축에서 분할이 하나도 안 늘어서 파고들었다. **v8(합계 줄)은 원인의
일부였을 뿐이고, 진짜는 넷이 더 있었다.** 전부 고친 뒤 경험 24 → **29**.

**1. `max_tokens: 1024` 가 답을 잘랐다.** `also`(추가 경험)는 출력 **끝**에
오는데 스키마상 optional 이라, 잘려도 검증을 통과한다 — 경험만 조용히 하나로
줄어든다. 온전히 답하는 데 1014~1030 토큰이 드는데 상한이 1024 였다.

```
temp=0 max_tokens=2000 → 경험 3·3·3   stop=tool_use
temp=0 max_tokens=1024 → 경험 3·1·1   stop=max_tokens
```

v6 이전엔 0건이다 — v7 이 압축 로그에 sec·via·earlier 를 실으면서 답이
길어졌고 v8 의 합계 줄이 더 밀었다. 2048 로 올리고, `stop_reason='max_tokens'`
면 `ingest_failures` 에 남기고 중단한다(조용히 틀리는 것보다 드러나는 게 낫다).

**2. 모델이 구간을 배타적으로 배정하지 않는다.**

```
주   segs [0..18]              19개 전부
+    segs [0,12,14]     22분   주와 겹친다
+    segs [3,5,6,8,9,10] 15분  주와 겹친다
```

프롬프트가 "한 구간은 한 경험에만" 이라 시키는데 안 지킨다 — 요약문이 실제로
전체를 훑으니 모델 셈으로는 맞는 말이다. 코드는 `seen` 에 걸려 통째로 하나로
되돌렸다. **문턱을 넘긴 경험 둘을 id 기록 방식 때문에 버린 것이다.**
곁가지끼리는 여전히 배타를 요구하되(둘이 다투면 그 시간이 어느 일인지 알 수
없다), **주 경험은 겹치는 것을 빼서 받는다** — 곁가지가 더 구체적인 주장이고
주는 나머지다.

**3. `segmentsOf` 가 `earlier` 를 건너뛰었다.** `earlier.top` 의 판이 둘인데
(배포된 확장은 문자열, 지금 빌더는 객체) 객체만 읽었다. 그래서 긴 세션의
앞부분이 분할 문턱에도 시간 배분에도 안 잡혔다 — 183분 세션이 **70분**으로
계산됐다. 고치니 181분. (`buildTargetTotals` 도 같은 이유로 둘 다 읽는다.)

**4. `sec` 없는 옛 세션은 시간을 못 믿는다.** `secOf` 의 시각차 폴백은 주석대로
"**비율로만** 쓰므로 없는 것보다 낫다" 인데 `MIN_SPLIT_SEC` 는 절대값(600초)
으로 쓴다. 20분 세션에서 네이버 구간이 601초로 잡혀 **1초 차이로** 문턱을
넘었고 `"검색으로 전환되었다."` 라는 실체 없는 경험이 갈래까지 만들었다.
그 601초는 마지막 관측과 세션 종료(switch) 사이의 간격이지 체류가 아니다.
못 믿을 때는 프롬프트와 같은 편에 선다 — **기본은 나누지 않는 것이다.**
`sec` 은 2026-08-06 부터 들어오므로 새 세션에는 해당이 없다.

#### 최종 결과 (재구축 3회차)

```
경험 29 · 갈래 7 · 기억 3 · 스킬 14    (시작: 경험 24 · 갈래 6)
잘림 0건 · 교정 6건 고아 0 · 일기 죽은 id 0 · 쓰레기 경험 0
```

분할된 세션 4건(전엔 1건):

```
08-06 23:52 183분 → 3개   ← 원래 문제의 그 세션
08-07 03:21 241분 → 2개
08-07 08:15  47분 → 2개
08-07 11:34 126분 → 3개
```

#### 남은 흠

- **주 경험의 시간이 작아질 수 있다.** 겹침을 빼면 주는 "나머지" 라, 요약문은
  세션 전체를 말하는데 시간은 1분인 경우가 나온다(183분 세션). 요약을 다시
  짓게 하지 않는 한 어쩔 수 없다 — 표시상 어색하지만 시간 배분 자체는 맞다.
- **옛 세션 18건은 여전히 안 나뉜다**(`sec` 없음). 의도한 것이다.

### (첫 재구축 때의 기록) 분할은 안 늘었다 (경험 2개 이상 세션 1 → 1)

**v8 의 효과가 이번 데이터에서는 안 나타났다.** 픽스처 A/B 에서는 2.2 → 3.0
이었는데 실세션에서는 그대로다. 짚이는 이유:

- **`earlier` 가 있는 세션이 1건뿐이다.** 합계 줄이 가장 크게 달라지는 건
  앞부분이 잘린 긴 세션인데, 그런 세션이 거의 없다.
- 옛 세션은 `sec` 이 없어 시각 차이로 어림하는데 관측 1회 구간이 0 이 된다 —
  합계 자체가 실제보다 작게 잡힌다(08-06 이전).
- 그리고 **분할이 안 일어나도 갈래는 제자리를 찾았다.** 뿌리(183분 세션)가
  여전히 하나로 뭉쳐 있는데도 그렇다 — 「진행 중 갈래에 마지막 요약」 수정이
  따로 일했을 수 있다. 어느 쪽이 얼마나 기여했는지는 안 갈랐다.

새 세션이 쌓이면 다시 본다. 그때도 안 나뉘면 「10분 문턱 알려주기」(②)를
근거를 갖고 올린다.

---

## 대상별 체류 합계를 프롬프트에 넣었다 (2026-08-08)

분할이 모자랐던 건의 후속. **판정 규칙은 안 건드리고 정보만 더했다** —
"기본은 나누지 않는다"도, 10분 문턱을 알려주는 것도 아직 안 했다.

### 무엇을

유저 메시지에 한 줄 (`buildTargetTotals`):

```
- 대상별 체류 합계(segments·via·earlier 를 모두 더한 값):
  zep.us · … ZEP 68분 · localhost · Project NA 40분 · platform.claude.com · … 14분 · …
```

바로 위 `방문 도메인` 줄과 다르다 — 그건 도메인만이라 `localhost: 2485초` 가
Project NA 인지 SOLDIER 인지 모른다. 이 줄은 **도메인+제목**이라 대상이 갈린다.

시스템 프롬프트에도 읽는 법을 적었다: *"같은 대상도 나갔다 오면 구간이 새로
생기므로 타임라인만 보면 무엇이 큰 일이었는지 놓치기 쉽다. 이 세션에 큰 대상이
둘 이상이었는지는 이 줄로 판단하라."*

### 짚은 것들

- **웹 쪽에서 계산한다.** 확장 배포가 필요 없고 과거 세션에도 소급된다.
- **`earlier.top` 판이 둘이다.** 배포된 확장은 문자열(`"zep.us · … 46분"`),
  지금 빌더 코드는 `{i,label,sec}` 객체를 만든다. **둘 다 파싱한다** —
  한쪽만 보면 긴 세션의 앞부분이 통째로 안 세어진다. 실제로 이것 때문에
  내가 처음 계산을 틀렸다(Project NA 를 6분으로 봤는데 46분이었다).
  ※ `segmentsOf` 도 같은 문제를 안고 있다 — 객체만 읽어서 실데이터에서는
    `earlier` 를 건너뛴다. 즉 **코드의 10분 문턱도 앞부분을 못 세고 있다.**
    (분할이 실제로 일어날 때만 쓰이는 값이라 아직 안 고쳤다.)
- **`via` 는 안 센다.** 시간이 없어 30초로 어림해봤더니 스쳐간 횟수가 많은 곳이
  부풀었다 — `www.naver.com · NAVER` 가 12분으로 잡혀 "큰 대상" 자리에 올라왔다.
  네이버는 거쳐가는 곳이지 일이 아니다. via 는 정의상 1분 미만이라 어차피
  문턱 근처에 못 가고, 흔적은 타임라인에 그대로 남는다.
- **옛 세션(`sec` 없음)은 시각 차이로 어림**한다(`secOf` 와 같은 규칙).
  관측이 한 번뿐인 구간은 0 이라 실제보다 작다 — 08-06 이전 세션은 10분 넘는
  대상이 거의 안 잡힌다. 순위만 맞으면 되는 값이라 그대로 뒀다.

### 검산 (최근 14세션, LLM 안 부름)

전 세션에서 줄이 나온다. 문제의 183분 세션: `ZEP 68분 · Project NA 40분` —
**두 대상이 나란히 보인다.** 예전에는 이걸 알려면 모양이 다른 두 목록에 걸친
산수를 해야 했다.

### eval — 회귀 없음, 그리고 효과 확인

**골든셋 14/17.** 기준선과 점수도 실패 항목도 같다(`success` · `partial` ·
`thread new` — 원래 상시 실패). 분포도 그대로. 51콜 · $0.73.

골든셋은 분할 개수를 안 잰다(`expect` 가 outcome·category·thread.action 만
본다). 그래서 따로 쟀다 — **문제의 183분 세션을 픽스처로 재현**해서:

```
그때(실제)      경험 1개 · segment_ids [0..18]   ZEP+Project NA+쇼핑+Claude 를 하나로
지금 3회         경험 4 · 3 · 4개                 ZEP 행사 / Project NA / Claude / 쇼핑
```

**A/B 로 원인을 갈랐다** (합계 줄과 시스템 프롬프트의 설명 문단만 빼고 5회씩):

```
합계 줄 없음 → 2 · 2 · 2 · 2 · 3   평균 2.2
합계 줄 있음 → 3 · 3 · 3 · 3 · 3   평균 3.0   ← 편차도 사라졌다
```

**단정하지 말 것 둘:**
- 픽스처는 원본과 다르다(`earlier` 를 3줄로 줄이고 구간도 12개만 넣었다).
- **3개가 맞는 답인지는 모른다.** ZEP·Project NA·쇼핑으로 갈렸으면 맞지만,
  4개 낸 회차는 `Claude 도구`(11분)와 `쇼핑`(8분)을 따로 뺐다 — 문턱 근처라
  애매하다. 과분할 쪽으로 기울 여지가 있으니 실사용에서 지켜본다.

「10분 문턱 알려주기」(②)는 **안 넣었다.** ① 만으로 움직였으므로 근거가 더
쌓이기 전에는 판정 규칙을 안 건드린다.

---

## 갈래 교정 (2026-08-08 · 구현 완료, 아직 아무것도 안 고침)

`corrections` 에 `thread` 필드를 넣었다. `/diary` 에서 갈래를 고르거나 새
이름을 적으면 경험이 실제로 옮겨지고, 재구축해도 유지된다.

### 왜 만들었나 — 실측된 연쇄 오염

```
08-04 13:16 [Project NA 개발]  GitHub 커밋 히스토리          ← 진짜
08-04 23:58 [Project NA 개발]  Army Sim + ZEP kt cloud       ← 여기서 처음 잘못 붙음
08-05 02:11 [Project NA 개발]  Project NA … ZEP 을 오가며     ← "최근:" 줄이 오염됨
08-05 23:54 [Project NA 개발]  KT Cloud TECH UP 2기 행사      ← 목록이 ZEP 을 말하니 붙음
08-06 01:01 [Project NA 개발]  KT Cloud 행사 중 Figma…        ← 굳어짐
```

한 번 잘못 붙으면 그 갈래의 `최근:` 줄이 다음 판정을 끌어당긴다. 프롬프트에
"분야가 같다는 건 attach 의 근거가 아니다 / 애매하면 new" 가 **이미 있는데도**
안 걸린다 — 최근 줄이 실제로 그 주제를 말하고 있으면 규칙이 걸릴 자리가 없다.

같은 세션의 재구축별 판정(temperature 0 인데 뒤집힘):

```
1회차  주 → new "KT Cloud TECH UP 2기 행사 참여"   ✔
2회차  주 → new "KT Cloud TECH UP 2기 참여"        ✔
3회차  주 → attach Project NA(10건)                ✘
```

앞선 세션들이 더 잘 쪼개지면서 Project NA 가 10건까지 커졌고, **큰 갈래가 더
강한 유인이 됐다.** 쪼개기를 고친 것이 이쪽을 악화시켰다.

### 쪼개기로는 못 고친다 (앞선 진단이 틀렸다)

처음엔 "`sec` 없으면 안 나눔"(fix#4)이 교정용 `also` 를 지운 탓이라 봤는데,
재보니 아니었다:

```
08-05 23:54  57분 세션인데 측정된 체류 총합 5분  → also 0.3분
08-06 01:01  53분 세션인데 측정된 체류 총합 2분  → also 0분 (segment_ids 가 [])
```

옛 세션은 체류시간이 사실상 없어 **10분 문턱을 절대 못 넘는다.** fix#4 를
되돌려도 살아나는 건 3건이고 KT Cloud 는 거기 없다. 오염은 쪼개기가 아니라
**주 경험의 갈래 판정 자체**에서 났다.

### thread 만 다르게 다룬 이유

나머지 세 필드는 라벨이라 읽을 때 겹치면 되지만, 갈래는 **관계**다. 후보
목록·`최근:` 줄·경험 개수가 전부 `thread_id` 조인에서 나온다. 그래서
`experiences.thread_id` 를 실제로 옮기고 양쪽 갈래의 파생값을 다시 만든다
(`lib/corrections.ts` 의 `moveExperienceToThread`). `(모델 출력, 사람 정답)`
쌍은 `model_value` 에 옮기기 전 제목을 박제해 그대로 남는다.

`human_value` 는 uuid 가 아니라 **제목**이다 — 재구축이 `threads` 를 통째로
다시 만들어 uuid 가 매번 새로 발급된다. `rebuild.mts` 는 교정 행을 되돌린
자리에서 옮기기도 다시 적용한다. 행만 되돌리면 갈래는 원래대로 돌아간다.

### 남은 것

- **아직 아무 갈래도 안 고쳤다.** 위 3건(08-04 23:58 · 08-05 23:54 ·
  08-06 01:01)은 여전히 Project NA 에 있다 — 사용자가 `/diary` 에서 직접 한다
- 고치고 나면 **"Project NA 개발" 기억의 근거 9건 중 3건이 다른 갈래를
  가리키게 된다.** `memories.experience_ids` 는 안 따라가므로 그 기억을 다시
  만들지, 근거만 걷어낼지 정해야 한다
- 마이그레이션 `20260808000001_thread_correction.sql` 은 **원격 DB 에 이미 적용**했다

## 안 끝난 것

### 1. `outcome = success` 가 역대 0건

`llm_outputs` **92회** 호출 동안 한 번도 안 나왔다(`partial 41 · explore 41 · stuck 10`).
골든셋에서는 잘 나오므로 **프롬프트가 막는 게 아니라 실제 세션이 그렇게 안
생겼을** 가능성이 크다.

- 확인용 스크립트를 써뒀는데 **안 돌렸다**: `apps/web/scripts/probe-success.mts`
  - 실데이터 문맥은 그대로 두고 세션만 "확실한 성공 → 애매 → 실제 세션 모양"
    순으로 바꿔가며 어디서 `success` 가 꺼지는지 경계를 찾는다
- 돌려서 ①에서도 안 나오면 규칙 문제, 나오면 데이터가 그런 것이다
- 영향: `brokeThrough`(+35)와 `breakthrough` 트리거가 구조적으로 발동 불가

### 2. 빈 갈래 — 경험 상한과 갈래 상한이 어긋난다

```
apps/web/src/app/page.tsx   경험 limit(400) · 갈래 limit 없음
```

갈래는 다 그리는데 그 갈래의 경험이 상한 밖이면 **눌러도 위성이 0개**다.
판독값에 '경험 0건' 으로 뜨고, 눌러도 안 열린다.

- 상한이 220 이던 시절 6개월 시드로 56개가 그랬다. 400 으로 올려(갈래 화면을
  없애면서) 줄었지만 원인은 그대로다 — 하루 8세션이면 약 두 달 뒤부터 다시 는다
- 고치는 방향(정한 것): 상한을 없애되 **위성 배치에 필요한 필드만** 내려받는다
  ```
  id · occurred_at · category · outcome · memory_score · thread_id · is_first_time
  ```
  요약문(`summary`·`detail`)은 프로브가 뜰 때만 필요하다. 좌표만이면 경험당
  100바이트 미만이라 3000건도 300KB 안쪽이다
- **하지 말아야 할 것**: 갈래를 같이 자르는 것. 갈래는 다 보여야 한다 —
  지도가 하나뿐이라 여기서 안 보이면 그 일은 사이트 어디에도 없다
- 전환이 어색해지는 문제: 클릭 전에 반드시 겨누므로(조준점이 빨려드는 시간)
  **호버 시점에 미리 불러오면** 빈 화면이 안 난다. 안 왔으면 이미 가진 것으로
  먼저 그리고 이어 붙인다
- **지금 고치지 않기로 했다.** 아직 안 아프고, 실제 크기를 보고 설계하는 게 낫다

### 3. `close_reason` 을 사람이 고칠 장치가 없다

`outcome`·`category`·`is_first_time` 은 `/diary` 에서 고칠 수 있는데 세션 경계는
못 고친다. 그리고 고쳐도 **반영될 자리가 없다** — 확장의 규칙은 하드코딩 상수다.

다만 실측으로 **치명적이지 않다**:

```
switch 로 끊긴 뒤 같은 갈래로 이어진 비율   8/12
```

세션이 잘못 쪼개져도 갈래에서 다시 봉합된다. 남는 피해는 (a) 나머지 4/12,
(b) `duration_min` 이 잘려 성격의 몰입형 축이 깎이는 것, (c) 조각이 10분 미만이면
전송 필터에 걸려 **통째로 사라지는 것**(되돌릴 방법 없음).

- 팝업이 이제 버려진 세션을 한 줄씩 보여준다(길이·마감사유·탈락사유)
- `맥락이탈 · 10분 미만` 이 쌓이면 끊는 규칙이 작업을 죽이고 있다는 신호다.
  그때 규칙 변경을 꺼낸다: "끊김 없이 10분" → "최근 15분 창에서 비중 70%"
- 2026-08-06 확인: 버려진 2건이 `무활동 · 10분 미만` 과 `활동 점수 부족` 이라
  **규칙이 죽인 게 아니다.** 근거 부족으로 변경 보류

### 4. `design` 카테고리가 프롬프트에 없다 — 다만 실제로는 나온다

프롬프트는 **열셋만 나열**하는데(design 이 빠졌고 문장도 "아래 열셋 중") 툴
스키마 enum 에는 있다. 그래서 안 나올 줄 알았는데 실세션 프로브에서 두 번
나왔다("Figma 에서 아이디어톤 프로젝트의 디자인을 검토했다" → design).

- 급하지 않다. 넣는다면 프롬프트 한 줄 + `PROMPT_VERSION` + 골든셋 재실행
- 카테고리 재판정은 프롬프트 한 줄로 무너진 적이 있으니 **eval 없이 올리지 않는다**

### 6. 분할이 과하게 나오는지 지켜볼 것

실세션 5건에서 모델이 매번 2~4개를 냈고, 10분 문턱이 걸러 2개로 수렴했다.
경험 수가 늘면 레벨 공식(√(경험 수 × 스킬 수))이 부푸는데, 지금은
`가입 일수/3 + 1` 이 캡이라 안 튄다 — **가입 한 달이 지나면 그 방어막이 풀린다.**

- 재는 법: 세션당 경험 수 분포. 2개가 흔하고 3개가 드물면 정상
- 3개가 흔해지면 `MIN_SPLIT_SEC`(10분)을 올린다
- `also` 는 툴 스키마에서 **optional 이어야 한다.** required 로 두면 모델이 매
  호출에 채우느라 다른 판정이 밀린다(골든셋 15 → 13)

### 5. 탭 전환만으로 최대 10분이 귀속된다

`tabSwitch` 가 5점짜리 점수 이벤트라, **켜기만 하고 아무것도 안 한 탭**도 다음
활동까지의 간격(최대 10분)을 가져간다. "켜놓고 방치한 건 안 센다"는 원래 의도와
어긋나는 자리다.

- 고치면 한 줄이다: 간격을 주는 이벤트에서 `tab_activated` 단독은 제외
- 다만 `switch` 판정과 `activity_score` 가 같은 값을 쓰므로 함께 흔들린다
- 실제로 얼마나 되는지는 원본에서 잴 수 있다(`replay-raw.mts` 옆에 붙이면 된다)
- **재보고 결정한다.** 지금은 근거가 없다

---

## 배포 순서 (2026-08-07 에 데인 것)

**스키마를 코드보다 먼저 올리지 않는다.** 그리고 **배포 성공을 눈으로 확인한다.**

이날 `session_id` UNIQUE 를 DB 에서 먼저 제거했는데 코드 배포가 계속 실패하고
있었다. 배포된 옛 엔진의 `on conflict (session_id)` 가 대응 제약을 못 찾아
**들어오는 세션이 전부 처리 실패**했다(2시간, 세션 1건). 빌드 실패는
`next build` 가 `scripts/` 를 타입 검사하다 확장 의존성(dexie)을 못 찾은 것인데,
로컬은 호이스팅돼서 통과하므로 **로컬 빌드로는 절대 안 걸린다.**

```
1. 코드 푸시 → vercel list 로 ● Ready 확인
2. 그 다음 마이그레이션 적용
3. 되돌릴 수 없는 변경(제약 제거)은 특히 이 순서를 지킨다
4. 처리 실패는 ingest_failures 와 processed_at IS NULL 로 즉시 드러난다 —
   스키마를 만졌으면 그날 한 번은 본다
```

옛 코드가 새 스키마에서 도는 창을 없애려면 순서가 반대여야 한다. 반대로 새
코드가 옛 스키마에서 도는 것은 대개 안전하다(추가 컬럼은 무시된다).

---

## 재볼 때 쓰는 도구

전부 `apps/web/scripts/`. LLM 을 부르는 것과 아닌 것을 구분해뒀다.

| 스크립트 | 하는 일 | LLM |
|---|---|---|
| `replay-raw.mts` | 콜드 스토리지 원본을 **지금 압축기**에 다시 태운다 | X |
| `probe-split.mts` | 원본 → 압축 → LLM → 분할 계획까지 (DB 안 씀) | O |
| `probe-e2e-paths.mts` | 저장까지 — 분할·attach·교정·멱등성·deepened | O |
| `probe-e2e-split.mts` | 저장까지 — 세션 하나를 격리 유저로 (부활 포함) | O |
| `audit-close-reason.mts` | 세션 마감 사유가 로그와 맞는지 되짚는다 | X |
| `unmapped-domains.mts` | 카테고리 표에 없는 도메인을 체류 시간 순으로 | X |
| `seed-6months.mts` | 목데이터(`--apply` / `--years` / `--clear`) | X |
| `probe-candidates.mts` | 잠긴 갈래 후보를 FTS 가 제대로 고르는지 | X |
| `probe-revive.mts` | 모델이 잠긴 갈래에 붙이는지 | O |
| `probe-complete.mts` | 어떤 세션을 완결로 보는지 | O |
| `probe-success.mts` | `success` 가 어디서 꺼지는지 (**미실행**) | O |
| `eval-prompt.mts` | 프롬프트 골든셋 17케이스 | O |
| `rebuild.mts` | 파생 전부 지우고 세션에서 재구축 | O |
| `dry-reprocess.mts` | 새 프롬프트가 뭘 뱉는지. DB 안 건드림 | O |

`audit-close-reason` 과 `unmapped-domains` 는 확장의 `categorize` 를 직접
import 한다 — 저장된 로그의 `category` 는 그때의 표로 매긴 값이라, 표를 고치고
나면 옛 판정을 되짚게 된다.

### 재구축 뒤 반드시 할 것

```
apps/batch/scripts/rebuild-diary.mts <날짜> --apply
```

`daily_logs.experience_ids` 는 FK 가 없어서 재구축 뒤 죽은 id 를 물고 남는다.
안 하면 `/diary` 에서 근거가 통째로 사라진다. 실제로 세 번 빠뜨렸다.

---

## 다음 순서 (정해둔 것)

1. ~~구간 절단 — 압축 로그가 세션 앞부분을 통째로 버리던 것~~ **완료**
2. ~~**경험 분할** — 한 세션에 대상이 둘 이상이면 경험을 나눈다~~ **완료** (2026-08-07)
   - 세션은 그대로 둔다(관측 사실). 나누는 건 해석 층이다 — 확장이 도메인으로
     끊으면 `저장소 → 배포 → 로컬` 같은 **같은 일의 표면 이동**까지 잘려 나가고,
     그게 `success` 판정의 유일한 근거다
   - 모델이 구간을 대상별로 배정(`segment_ids`)하고, **시간·문턱은 코드가** 잰다
   - 대상별 `sec` 합이 10분 이상이면 분리, 미만이면 주 경험에 흡수(요약은 detail 로)
   - 배정 안 된 시간은 어디에도 안 넣는다 — 해석이 관측 전부를 덮을 필요는 없다
   - `occurred_at`·`duration_min` 은 배정된 구간에서 각자 계산
   - 선행 작업: `experiences.session_id` UNIQUE 해제. **그게 지금 멱등성 장치를
     겸하고 있다** — 풀기 전에 `sessions.processed_at` 조건부 UPDATE 로 갈아끼운다
   - 실측 근거: 대상이 둘 이상 섞인 세션 6/18, 그중 셋이 Project NA + Army Sim.
     셋 다 Project NA 갈래로 흡수됐고 Army Sim 갈래는 경험 2건으로 과소 기록됐다

---

## 정한 것 (다시 꺼내지 말 것)

- **갈래 반경은 `started_at` 기준.** `last_activity_at` 으로 바꾸는 안은 검토했고
  안 하기로 했다
- **위성이 많아 붐비는 것은 확대로 해결.** 상위 N개만 그리는 안은 안 넣는다
- **`completed` 는 모델에게 묻지 않는다.** 역대 75회 중 0건이고, 완결 예시를
  그대로 재현한 세션도 `false` 였다. 사람이 지도에서 별을 당겨 '끝'을 누른다
- **`COMPANION` 을 시간으로 제한하는 안은 뺐다.** 표본 1건에 결과도 안 바뀐다

---

## 세션 한도 대비

대형 배치를 띄우기 **전에** 이어가기 예약을 걸어둔다. 한도 오류를 본 뒤에는
메인 루프까지 막힐 수 있다. 예약 프롬프트에 반드시 포함할 것:

- 배치마다 테스트, 배치 완료 후 빌드 1회
- 재구축했으면 일기 재생성
- 이 파일 갱신 및 결과 보고
- 못 끝내면 같은 규칙으로 다음 예약을 스스로 생성
