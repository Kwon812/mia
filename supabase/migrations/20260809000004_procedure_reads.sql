-- ============================================================
-- 절차에 "여기서 무엇을 확인하는가"를 붙인다.
--
-- 클릭은 기록되지만 **본 것은 기록되지 않는다.** 아침에 배포 상태를 눈으로
-- 확인했다면 남는 건 그 페이지를 열었다는 사실뿐이고, 무엇을 봤는지는
-- 관측 대상이 아니다 — 눈은 이벤트를 안 만든다.
--
-- 그래서 이건 사람이 말해줘야 하는 두 번째 것이다. 첫 번째는 "자동화할
-- 값어치가 있나"(귀찮음이 관측되지 않는다), 두 번째가 이것이다.
--
-- 왜 값어치가 큰가: 이게 없으면 이동만 자동화된다. 네 군데를 순서대로
-- 열어주고 보는 건 여전히 사람이 한다 — 4분이 3분이 될 뿐이다. 읽기가
-- 있으면 네 군데 상태가 한 화면에 모여 10초에 끝난다.
-- ============================================================

alter table procedures add column if not exists reads jsonb not null default '[]'::jsonb;
